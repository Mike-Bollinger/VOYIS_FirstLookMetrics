# Core GUI and System Libraries
#tkinter  # Usually comes with Python, but listed for completeness

# Image Processing and Computer Vision
Pillow>=9.0.0
opencv-python>=4.5.0

# Scientific Computing and Data Analysis
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0
rtree

# Plotting and Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Geographic Data Processing (Optional but recommended)
geopandas>=0.10.0
shapely>=1.8.0

# Machine Learning and Deep Learning (Used in visibility analysis)
scikit-learn>=1.0.0

# TensorFlow - Required for visibility analysis model (inference + training)
# Windows native: pip install "tensorflow<2.11" (last version with Windows GPU support)
#   + conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0
# Windows WSL2 / Linux (GPU): pip install tensorflow[and-cuda]
# CPU only (any platform): pip install tensorflow
# NOTE: tensorflow-intel (Intel CPU build) does NOT support NVIDIA GPUs.
tensorflow>=2.10.0

# Date/Time Processing
python-dateutil>=2.8.0

# Enhanced GUI Features (Optional)
tkinterdnd2>=0.3.0

# ROS2 MCAP bag file reading (for turbidity data from R620 AUV bags)
mcap>=1.0.0

# File and Path Utilities (Python 3.7+ has pathlib built-in, but this ensures compatibility)
pathlib2>=2.3.0; python_version<"3.4"